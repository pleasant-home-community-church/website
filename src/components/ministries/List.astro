---
import Item from '~/components/ministries/ListItem.astro';
import type { Ministry } from '~/types';

export interface Props {
  posts: Array<Ministry>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} />
      </li>
    ))
  }
</ul>
